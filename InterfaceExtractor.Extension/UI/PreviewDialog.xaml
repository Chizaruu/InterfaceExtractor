<Window x:Class="InterfaceExtractor.UI.PreviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Interface Preview"
        Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        ShowInTaskbar="False">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="20,8" />
            <Setter Property="Margin" Value="5,0" />
            <Setter Property="MinWidth" Value="100" />
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Preview the generated interface before saving"
                   FontWeight="Bold"
                   FontSize="14"
                   Margin="0,0,0,10" />

        <!-- File Info -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Interface: " FontWeight="SemiBold" Margin="0,0,10,5" />
            <TextBlock Grid.Row="0" Grid.Column="1" x:Name="InterfaceNameText" Margin="0,0,0,5" />

            <TextBlock Grid.Row="1" Grid.Column="0" Text="File: " FontWeight="SemiBold" Margin="0,0,10,0" />
            <TextBlock Grid.Row="1" Grid.Column="1" x:Name="FilePathText" />
        </Grid>

        <!-- Code Preview -->
        <Border Grid.Row="2"
                BorderBrush="#CCCCCC"
                BorderThickness="1"
                Background="#F8F8F8"
                Margin="0,0,0,15">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto">
                <TextBox x:Name="PreviewTextBox"
                        IsReadOnly="True"
                        FontFamily="Consolas"
                        FontSize="12"
                        TextWrapping="NoWrap"
                        BorderThickness="0"
                        Background="Transparent"
                        Padding="10"
                        VerticalScrollBarVisibility="Auto"
                        HorizontalScrollBarVisibility="Auto" />
            </ScrollViewer>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="3"
                   Orientation="Horizontal"
                   HorizontalAlignment="Right">
            <Button Content="Save"
                   IsDefault="True"
                   Click="Save_Click"
                   ToolTip="Save the interface to disk" />
            <Button Content="Cancel"
                   IsCancel="True"
                   Click="Cancel_Click"
                   ToolTip="Cancel without saving" />
        </StackPanel>
    </Grid>
</Window>